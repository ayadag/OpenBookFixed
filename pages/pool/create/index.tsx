"use client";
import CreatePool from '../../../components/createPool';
import Layout from '../../../components/layouts/layout';

const Pool = () => {
    return(
        <Layout>
            <CreatePool/>
        </Layout> 
    )
}
export default Pool;